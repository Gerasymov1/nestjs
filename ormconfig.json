{
  "type": "mariadb",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "",
  "database": "root",
  "entities": ["src/**/*.entity{.ts,.js}"],
  "synchronize": false,
  "migrations": ["src/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}